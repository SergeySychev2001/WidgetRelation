{
    "compilerOptions": {
        // Указывает, что результат компиляции должен быть совместим с ES6
        "target": "ES6",
        // Указывает библиотеки, которые должны быть включены в проект
        "lib": ["DOM", "ES6"],
        // Включает набор правил, направленных на усиление проверки типов и предотвращение ошибок
        "strict": true,
        // Включает поддержку синтаксиса импорта для CommonJS и ES6 модулей.
        "esModuleInterop": true,
        // Пропускает проверку типов в файлах деклараций типов (.d.ts), что может ускорить компиляцию, особенно в больших проектах.
        "skipLibCheck": true,
        // Указывает, что код должен быть скомпилирован с использованием модуля ESNext, что позволяет использовать современные возможности JavaScript, такие как динамические импорты.
        "module": "ESNext",
        // Указывает, что при разрешении модулей следует использовать стратегию, аналогичную Node.js.
        "moduleResolution": "node",
        // Позволяет импортировать файлы JSON как модули.
        "resolveJsonModule": true,
        // Включает проверку на неиспользуемые локальные переменные в коде.
        "noUnusedLocals": true,
        // Включает проверку на неиспользуемые параметры функций.
        "noUnusedParameters": true,
        // Запрещает "проваливание" случаев в switch-операторах, что означает, что если один случай не завершен явно через break, то TypeScript выдаст ошибку.
        "noFallthroughCasesInSwitch": true,
        // Запрещает функции без явных возвращаемых значений. То есть, если функция должна возвращать значение, то она должна обязательно это делать.
        "noImplicitReturns": true,
        // Отключает обрезку сообщений об ошибках, позволяя выводить полные сообщения о компиляции.
        "noErrorTruncation": true,
        // Позволяет компилировать каждый файл как независимый модуль (важно для инструментов, таких как Babel),
        // а также предотвращает использование возможностей, которые могут нарушить независимую компиляцию (например, const enum).
        "isolatedModules": true,
        // Пропускает проверку типов стандартной библиотеки, что может ускорить процесс компиляции.
        "skipDefaultLibCheck": true,
        // Указывает TypeScript, где искать глобальные типы.
        "typeRoots": ["node_modules/@types"],
        // Настройка псевдонимов путей
        "paths": {
            "@/*": ["*"]
        },
        // Точка входа для tsconfig
        "baseUrl": "src"
    },
    "exclude": ["node_modules"],
    "include": ["**/*"]
}
